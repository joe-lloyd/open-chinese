# SERVER-ONLY. Set these as Netlify environment variables — never in the repo,
# never in a .env file that gets committed, and never with a VITE_ prefix.
#
# A VITE_ prefix would inline the value into the browser bundle. None of the
# values below may ever be prefixed that way. The public, browser-safe variables
# live in client/.env.example.
#
# None of this is wired to a live account yet: provider selection and account
# setup are still open decisions. Leaving PAYMENT_PROVIDER unset makes every
# payment endpoint answer 503 and the app behave exactly as it did before.

# ── Which provider is live ──
# One of: stripe | coinbase-commerce
# Unset = payments not configured.
#
# MVP is `stripe`: test mode needs no application, so a full purchase can be
# exercised today. The longer-term recommendation is still a merchant of record
# (Polar or Paddle) so EU VAT registration and remittance are handled for you —
# see openspec/changes/add-monetization/design.md, decision D5. That swap is an
# account migration, not a rewrite: a provider is four methods behind the
# PaymentProvider interface.
PAYMENT_PROVIDER=

# Absolute origin used to build checkout return URLs, e.g. https://openchinese.app
# Netlify sets URL automatically in production; set this for local dev.
PUBLIC_SITE_URL=

# ── Firebase Admin ──
# A service account JSON key, either raw or base64-encoded.
#
# Create a DEDICATED service account with the "Cloud Datastore User" role only —
# not Owner, not Editor. This key is the one credential that can write
# entitlements, so scope it tightly and rotate it independently of everything
# else.
FIREBASE_SERVICE_ACCOUNT=

# ── Stripe (reference implementation) ──
STRIPE_SECRET_KEY=
# From the webhook endpoint's signing secret (whsec_...). Required — the webhook
# handler rejects every request when this is unset.
STRIPE_WEBHOOK_SECRET=
# Price IDs, created in the Stripe dashboard. One per catalogue SKU.
#
# Only the subscription is needed for the MVP — the pricing page offers packs
# only if they are listed in OFFERED_SKUS (client/src/lib/catalog.ts), and that
# list is currently the subscription alone. Leave the HSK price IDs unset until
# you decide to sell packs; the gating code already handles them.
STRIPE_PRICE_PRO_YEARLY=
STRIPE_PRICE_HSK_1=
STRIPE_PRICE_HSK_2=
STRIPE_PRICE_HSK_3=
STRIPE_PRICE_HSK_4=

# ── Coinbase Commerce (crypto, optional) ──
# No merchant of record supports crypto, so this runs alongside a card provider
# rather than replacing one. Note that a Pro subscription bought with crypto is a
# prepaid, non-renewing year — crypto has no recurring billing.
COINBASE_COMMERCE_API_KEY=
COINBASE_COMMERCE_WEBHOOK_SECRET=
# Decimal euro amounts, e.g. 25.00
CRYPTO_PRICE_PRO_YEARLY=
CRYPTO_PRICE_HSK_1=
CRYPTO_PRICE_HSK_2=
CRYPTO_PRICE_HSK_3=
CRYPTO_PRICE_HSK_4=

# ── Webhook endpoint to register with the provider ──
# https://<your-site>/.netlify/functions/webhook
#
# Stripe events to subscribe to:
#   checkout.session.completed
#   customer.subscription.created
#   customer.subscription.updated
#   customer.subscription.deleted
#
# Coinbase Commerce: charge:confirmed
