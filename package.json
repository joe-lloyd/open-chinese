{
  "name": "open-chinese",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter client dev",
    "build": "pnpm --filter client build",
    "build:words-db": "pnpm --filter open-chinese-scripts build:words-db",
    "typecheck:functions": "tsc -p tsconfig.functions.json"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "stripe": "^22.3.2"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "concurrently": "9.1.2",
    "typescript": "~6.0.2"
  }
}
